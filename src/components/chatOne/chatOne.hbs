  <div class="chat">
    {{#if selectedId}}
        {{{ chatInfoBlock }}}
        {{{chatMessageBlock}}}
        <div class="chat_new-message_form">
          {{{newMess}}}
        </div>
         {{else}}
             <div class="chat_message_start">Select or create a chat</div>
      {{/if}}
  </div>
